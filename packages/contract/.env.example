# Preprod deployment
MIDNIGHT_NETWORK=preprod
MIDNIGHT_INDEXER=https://indexer.preprod.midnight.network/api/v4/graphql
MIDNIGHT_INDEXER_WS=wss://indexer.preprod.midnight.network/api/v4/graphql/ws
MIDNIGHT_NODE=https://rpc.preprod.midnight.network
PROOF_SERVER=http://localhost:6300
# Wallet seed (24 words or 64 hex) — never commit real value
MIDNIGHT_WALLET_SEED=abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about
CONTRACT_ADDRESS=
